/* glossary.js — short explanations for the terms the planner uses, with typical values and what low or high means.
 * Shown as tooltips (hover, focus or tap the dotted term) and listed in the Method view. */
const GLOSSARY = {
  // hardware
  accelerator:   { t: 'Accelerator', d: 'The GPU, TPU or other chip that runs the model. For serving, four numbers matter: memory (holds weights and KV cache), memory bandwidth (decode speed), tensor compute (prefill speed) and the number formats it computes natively.', typ: 'Data-center parts: 80–288 GB HBM, 2–8 TB/s, 1–5 PFLOPS FP8. Workstation cards: 24–96 GB, ~1–1.8 TB/s.', lo: 'Less memory means fewer or shorter sessions; less bandwidth means slower tokens.', hi: 'More bandwidth per byte of weights is what makes a chip feel fast per user.' },
  count:         { t: 'How many accelerators', d: 'Total accelerators across every node. The planner divides them into replicas of TP × PP and reports any left idle.', typ: '8 (one node) for a team, 16–64 for a department, hundreds for a product.', lo: 'Below the model\'s minimum layout nothing loads.', hi: 'Past the memory limit extra accelerators only add replicas, which add throughput, not context.' },
  'node-gpus':   { t: 'Accelerators per node', d: 'How many accelerators share one fast fabric inside a server. Tensor parallelism should stay within that group.', typ: '8 (HGX H100/H200/B200, MI300X), 72 in an NVL72 rack, 4 in some servers, 1 for a workstation.', lo: 'Small nodes force pipeline parallelism across the network for big models.', hi: 'Large fabrics (NVL72) let TP or expert parallelism span dozens of GPUs.' },
  fabric:        { t: 'Fabric inside a node', d: 'The link between accelerators in one server, used by every tensor-parallel all-reduce (two per layer per token).', typ: 'NVLink/NVSwitch 900 GB/s (Hopper) to 1.8 TB/s (Blackwell) per GPU; Infinity Fabric ~900 GB/s; PCIe 5.0 x16 about 64 GB/s.', lo: 'PCIe-only boxes add several milliseconds per token at TP 8.', hi: 'With NVLink the collective cost is a couple of milliseconds per token, mostly latency.' },
  network:       { t: 'Network between nodes', d: 'Per-node aggregate bandwidth between servers. Carries pipeline stages, cross-node tensor parallel traffic and KV-cache transfers for disaggregated prefill.', typ: '3.2 Tb/s (8 × 400 Gb/s InfiniBand) in AI clusters; 100–400 GbE RoCE in good racks; 25 GbE in ordinary ones.', lo: 'Tensor parallel across nodes becomes latency-bound on slow networks; pipeline parallel still works.', hi: 'Fast RDMA networks make multi-node layouts and KV transfers cheap.' },
  'host-ram':    { t: 'Host RAM per node', d: 'CPU memory on each server used to park idle sessions\' KV cache (CPU offload, LMCache) and restore it over PCIe when the user returns.', typ: '512–2,048 GB per node.', lo: 'Too little and idle sessions get evicted and re-prefilled from scratch.', hi: 'Enough to hold every user\'s session keeps every turn warm.' },
  // model
  model:         { t: 'Model', d: 'The open-weight model to serve. Its shape (layers, heads, KV heads, experts, attention type) sets memory per token and compute per token.', typ: '8–70B dense for most teams; 100–1,000B mixture-of-experts for frontier-class quality.', lo: 'Small models give fast, cheap tokens but weaker reasoning.', hi: 'Large MoE models compute like a 20–40B model but need the memory of the full parameter count.' },
  engine:        { t: 'Serving engine', d: 'The software that runs the model (vLLM, SGLang, TensorRT-LLM). It decides which quantized formats and KV-cache dtypes actually have kernels on your hardware, so the planner applies its documented support matrix.', typ: 'vLLM or SGLang for most deployments; TensorRT-LLM for NVIDIA-only peak performance.', lo: '', hi: '' },
  'w-prec':      { t: 'Weight precision', d: 'Number format of the stored weights: BF16 = 2 bytes per parameter, FP8/INT8 = 1, INT4/FP4 ≈ 0.5. Lower precision frees memory for KV cache and speeds up decode when the hardware computes in that format natively.', typ: 'FP8 is the production default on Hopper, Blackwell and MI300; INT4/FP4 when memory is tight.', lo: 'Below 8 bits quality can drop, more for small models than large ones.', hi: 'BF16 is the reference quality but costs twice the memory of FP8.' },
  'kv-prec':     { t: 'KV cache precision', d: 'Format of the stored keys and values. FP8 halves the cache per token with rarely measurable quality loss. INT4 quarters it but risks long-context quality and is offered by few engines.', typ: 'FP8 in production; BF16 when quality must be identical to the reference.', lo: 'INT4 KV is a research setting; sliding-window layers are especially sensitive.', hi: 'BF16 KV doubles memory per token compared with FP8.' },
  layout:        { t: 'Parallel layout', d: 'How the accelerators are grouped: tensor parallel (TP) × pipeline parallel (PP) makes one replica; the rest of the count becomes extra replicas.', typ: 'TP = accelerators per node, PP 1, replicas = nodes.', lo: 'Fewer replicas with wider TP give more KV cache per request and faster single-user speed.', hi: 'More replicas give more aggregate throughput but duplicate the weights.' },
  tp:            { t: 'Tensor parallelism (TP)', d: 'Splits every layer\'s weights across N accelerators that work on each token together, multiplying memory bandwidth for one request. Needs a fast fabric because every layer ends with an all-reduce.', typ: '8 inside an HGX node; the smallest power of two that fits the model when latency matters less.', lo: 'TP 1 avoids collectives but each request sees one accelerator\'s bandwidth.', hi: 'TP beyond the node crosses the network and becomes latency-bound.' },
  pp:            { t: 'Pipeline parallelism (PP)', d: 'Puts consecutive layers on different accelerators. Cheap on the network (one hand-off per stage), but each token still flows through every stage and each stage re-reads its weights per micro-batch.', typ: '1 inside a node; 2–4 across nodes for models that do not fit one node.', lo: '', hi: 'More stages add bubble time and do not make a single request faster.' },
  replicas:      { t: 'Replicas', d: 'Independent copies of the model, each serving its own requests behind a load balancer.', typ: 'One per node once the model fits a node.', lo: 'One replica pools all KV cache for the longest contexts.', hi: 'Many replicas scale throughput linearly but each holds a full copy of the weights.' },
  'dp-attn':     { t: 'Data-parallel attention', d: 'With multi-head latent attention (DeepSeek, Kimi) or when TP exceeds the number of KV heads, plain tensor parallelism copies the KV cache onto every rank. Data-parallel attention gives each rank its own requests (with expert parallelism underneath) so each session is stored once.', typ: 'On for DeepSeek/Kimi at TP > 1 (vLLM --data-parallel-size, SGLang --enable-dp-attention).', lo: 'Off with MLA at TP 8 leaves an eighth of the KV capacity.', hi: '' },
  // workload
  users:         { t: 'Users', d: 'People or agents with an open session. Only a fraction of them have a request in flight at any moment (see usage pattern).', typ: '10–50 for a team tool, hundreds to thousands for a product.', lo: '', hi: '' },
  activity:      { t: 'Share of time active', d: 'Fraction of the time a user has a request in flight. Concurrent requests = users × this share.', typ: 'Chat 5–15%, assistants 20–30%, coding agents 50–80%, batch pipelines 100%.', lo: 'Chat users mostly read and type, so few requests overlap.', hi: 'Agents fire tool calls back to back and behave like batch load.' },
  ctx:           { t: 'Context per session', d: 'Tokens the model attends over for one session: system prompt, conversation, documents and generated output. It sets the KV-cache size per session and the attention cost per token.', typ: '4k–32k for chat, 64k–200k for coding agents, up to 1M for whole-repository or long-document work.', lo: 'Short contexts make memory cheap and prefill fast.', hi: 'KV cache grows linearly and prefill attention quadratically with context.' },
  prefix:        { t: 'Shared prefix', d: 'Tokens identical at the start of every session, such as a system prompt or a shared knowledge pack. With prefix caching they are stored once per replica.', typ: '500–8k for a system prompt; 50k+ for shared documents.', lo: '', hi: 'A large shared prefix is nearly free with prefix caching and expensive without it.' },
  'new-prompt':  { t: 'New prompt tokens per turn', d: 'Tokens the user adds in one turn: their message plus any tool output. Only these need prefilling when the session\'s KV cache was kept.', typ: '200–2,000 for chat; 2k–20k for agents pasting tool output.', lo: '', hi: 'Big turns raise time to first token and the prefill share of GPU time.' },
  output:        { t: 'Output tokens per turn', d: 'Tokens generated in one turn. Together with tokens per second it sets how long a request stays in flight.', typ: '200–1,000 for chat; 1k–8k for reasoning models that think before answering.', lo: '', hi: 'Long outputs keep requests in flight longer, raising concurrency for the same users.' },
  retention:     { t: 'Idle sessions\' KV cache', d: 'What happens to a session\'s KV cache between turns. Kept in GPU memory: instant next turn, but the memory holds every user. Parked in host RAM: restored over PCIe on the next turn. Evicted: the whole conversation is prefilled again (cold time to first token).', typ: 'Host RAM parking for long-context multi-turn work; eviction is fine for short chats.', lo: '', hi: '' },
  target:        { t: 'Minimum tokens per second per user', d: 'Decode speed one request should see. Reading speed is about 5–7 tokens per second; 20–30 feels fluid; agents streaming code benefit from 50 or more.', typ: '20 for chat, 30–60 for agents, any for batch.', lo: 'Below 10 users notice the wait between words.', hi: 'High targets cap how many requests share a replica.' },
  'ttft-max':    { t: 'Longest acceptable time to first token', d: 'Time from sending a turn to the first generated token: prefilling the new tokens, plus restoring a parked session.', typ: 'Chat expects under 1–2 s; long-document ingestion tolerates 10–60 s.', lo: '', hi: 'Long limits allow cold prefills of huge contexts on few accelerators.' },
  // optimizations
  'prefix-caching': { t: 'Prefix caching', d: 'Reuses the KV cache of an identical prompt prefix across requests instead of recomputing it. Automatic in vLLM and SGLang; it is what makes multi-turn and shared-prompt workloads cheap.', typ: 'On.', lo: '', hi: '' },
  spec:          { t: 'Speculative decoding', d: 'A small draft model, n-gram lookup or the model\'s own multi-token-prediction heads proposes several tokens; the large model verifies them in one step. Per-user speed rises 1.5–3× when decode is memory-bound; the gain fades at large batch sizes where compute saturates.', typ: 'Draft length 3–5 tokens, 60–85% accepted.', lo: 'Low acceptance (creative text, weak draft) wastes verification compute.', hi: 'High acceptance (code, repetitive text) approaches the draft length in tokens per step.' },
  'spec-k':      { t: 'Draft tokens', d: 'How many tokens the draft proposes per step. Verification costs compute proportional to draft length + 1.', typ: '3–5; MTP heads usually 1–3.', lo: '', hi: 'Long drafts pay off only with high acceptance.' },
  'spec-alpha':  { t: 'Accepted share', d: 'Probability that a drafted token is accepted by the large model. Expected tokens per step = (1 − α^(k+1)) / (1 − α).', typ: '60–85%.', lo: '', hi: '' },
  pd:            { t: 'Prefill / decode disaggregation', d: 'Runs prefill and decode on separate accelerator pools connected by a KV-cache transfer. Keeps token speed steady when long prompts arrive in bursts; costs extra accelerators and network bandwidth.', typ: 'Used at production scale (NVIDIA Dynamo, SGLang PD, vLLM PD connectors); rarely worth it below one node.', lo: '', hi: '' },
  // assumptions
  util:          { t: 'Memory the engine may use', d: 'Share of accelerator memory the engine allocates for weights, activations and KV cache (vLLM gpu_memory_utilization). The rest is headroom for the driver and spikes.', typ: '85–95%.', lo: 'Conservative values waste KV capacity.', hi: 'Above 95% risks out-of-memory under load.' },
  'overhead-gb': { t: 'Runtime overhead per accelerator', d: 'Fixed memory for the CUDA context, captured graphs and kernel workspaces.', typ: '1–3 GB.', lo: '', hi: '' },
  'overhead-frac': { t: 'Activation overhead', d: 'Peak activation and workspace memory during a prefill chunk, as a share of the weight shard on each accelerator.', typ: '2–6% with chunked prefill.', lo: '', hi: 'Very long prefill chunks or vision encoders push it up.' },
  frag:          { t: 'KV block fragmentation', d: 'KV cache lost to partially filled paged-attention blocks.', typ: '2–6% with 16-token blocks.', lo: '', hi: '' },
  'bw-eff':      { t: 'Achieved memory bandwidth', d: 'Fraction of peak HBM bandwidth that decode kernels actually reach.', typ: '60–85%; FlashAttention and fused MoE kernels sit near the top.', lo: 'Unfused or poorly tuned kernels land near 50%.', hi: '' },
  mfu:           { t: 'Achieved tensor throughput (MFU)', d: 'Fraction of peak tensor FLOPS that prefill and large-batch decode reach.', typ: '40–60% for dense BF16; lower for MoE, FP8 and FP4 kernels.', lo: 'Small batches and MoE routing keep utilization low.', hi: 'Above 70% is rare outside benchmarks.' },
  'coll-eff':    { t: 'Achieved link bandwidth in collectives', d: 'Fraction of the fabric\'s peak bandwidth an all-reduce sustains once latency is paid.', typ: '60–80%.', lo: '', hi: '' },
  'pp-bubble':   { t: 'Pipeline bubble', d: 'Idle time per extra pipeline stage while micro-batches fill and drain the pipeline.', typ: '5–15% per extra stage.', lo: '', hi: '' },
  'host-restore': { t: 'Host-to-GPU restore rate', d: 'PCIe transfer rate per accelerator when a parked session\'s KV cache is copied back.', typ: '15–25 GB/s on PCIe 5.0, 8–12 GB/s on PCIe 4.0.', lo: '', hi: '' },
  // results
  concurrent:    { t: 'Concurrent requests', d: 'Requests being decoded at the same moment. Limited by KV-cache memory at your context and by the per-user speed target.', typ: 'Tens per replica at 128k context; hundreds to a thousand at 4k.', lo: '', hi: '' },
  'ctx-at-load': { t: 'Context per session at your load', d: 'The largest context every resident session can have while this many sessions are resident, from the KV-cache pool.', typ: '', lo: 'Below the context you asked for means the plan does not fit.', hi: '' },
  'speed-per-user': { t: 'Speed per user', d: 'Tokens per second one request receives, the inverse of the inter-token latency. It falls as more requests share a replica because every step reads every request\'s KV cache.', typ: '20–60 tokens per second in production; 100+ alone on a fast node.', lo: 'Under 10 feels sluggish.', hi: '' },
  itl:           { t: 'Inter-token latency', d: 'Time between two consecutive tokens for one request; one decode step stretched by the prefill work interleaved with it.', typ: '15–50 ms.', lo: '', hi: '' },
  aggregate:     { t: 'Aggregate throughput', d: 'Output tokens per second across all requests and replicas. Cost per token follows directly from it.', typ: 'Hundreds to a few thousand per replica; more with small models or large batches.', lo: '', hi: '' },
  ttft:          { t: 'Time to first token', d: 'Delay before the first generated token of a turn: prefill of the new tokens on one replica\'s tensor-parallel group, plus restoring a parked session. Cold is the first turn or an evicted session, when the whole prompt is prefilled.', typ: '0.2–2 s warm; seconds to minutes cold at 100k+ contexts.', lo: '', hi: '' },
  'kv-per-session': { t: 'KV cache per session', d: 'Bytes of keys and values stored for one session: KV bytes per token × context (windowed layers cap at their window).', typ: '1–40 GB at 128k depending on the model; 25–330 GB at 1M.', lo: '', hi: '' },
  'kv-per-token': { t: 'KV bytes per token', d: '2 × layers × KV heads × head dim × bytes per element for standard attention; layers × latent dim for multi-head latent attention; zero for linear-attention layers. It decides how much context fits.', typ: 'Llama 70B 320 KB (BF16), Qwen3-235B 190 KB, DeepSeek-V3 70 KB, gpt-oss-120b 37 KB, Qwen3-Next 25 KB.', lo: 'Cheap KV makes long context and many users affordable.', hi: 'Expensive KV is why some 70B models cannot hold ten 128k sessions per node.' },
  'decode-step': { t: 'Decode step', d: 'One forward pass that produces one token for every active request; its duration is the inter-token latency.', typ: '10–50 ms.', lo: '', hi: '' },
  'mem-traffic': { t: 'Memory traffic', d: 'Bytes read per decode step: every touched weight plus every request\'s KV cache, divided by the achieved bandwidth. Decode is usually bound by this.', typ: 'Weights dominate at small batch, KV cache at long context.', lo: '', hi: '' },
  compute:       { t: 'Compute', d: 'FLOPs per step: 2 × active parameters per token plus attention over the context, divided by achieved tensor throughput. Dominates at large batch or very long context.', typ: '', lo: '', hi: '' },
  collectives:   { t: 'Collectives', d: 'All-reduce time per token with tensor parallelism: two per layer, latency-bound at ~15 µs on NVLink and 50–100 µs across a network.', typ: '1–3 ms per token on NVLink at TP 8.', lo: '', hi: '' },
  'prefill-share': { t: 'Prefill share', d: 'Fraction of accelerator time spent prefilling new prompts (chunked prefill interleaves it with decode). Every decode step stretches by 1 / (1 − share).', typ: '5–30%.', lo: '', hi: 'Above ~85% the replica is saturated: prompts arrive faster than they can be computed.' },
  'weights-mem': { t: 'Weights', d: 'Model parameters in the chosen precision, split evenly across the replica\'s accelerators.', typ: 'Parameters × bytes per parameter: 70B in BF16 = 141 GB.', lo: '', hi: '' },
  'kv-pool':     { t: 'KV cache pool', d: 'Memory left for KV cache per replica after weights, runtime overhead and headroom.', typ: '', lo: 'A small pool means few or short sessions; lower weight precision grows it.', hi: '' },
  overhead:      { t: 'Runtime overhead', d: 'Per-accelerator memory for the CUDA context, graphs, workspaces and activation peaks.', typ: '2–6 GB.', lo: '', hi: '' },
  headroom:      { t: 'Headroom', d: 'Memory the engine leaves untouched (the share above the utilization setting).', typ: '5–15%.', lo: '', hi: '' },
  'cost-token':  { t: 'Cost per million output tokens', d: 'Hourly price of all accelerators divided by output tokens generated per hour. Compare with API prices, which range from about $0.10 to $15 per million.', typ: '$0.5–$5 for well-loaded self-hosting; far higher for lightly loaded long-context setups.', lo: '', hi: '' },
  'cost-hour':   { t: 'Cost per hour', d: 'Sum of the approximate on-demand cloud rates for every accelerator in the layout.', typ: '', lo: '', hi: '' },
  kw:            { t: 'Power', d: 'Accelerator power at rated TDP. Add roughly 30% for CPUs, memory, fans and cooling.', typ: '5–10 kW per 8-GPU node.', lo: '', hi: '' },
  'kv-repl':     { t: 'KV replication', d: 'Copies of the KV cache kept across tensor-parallel ranks when TP exceeds the KV heads or the model uses MLA.', typ: '1 with data-parallel attention.', lo: '', hi: '' },
  // catalog
  'tflops-bf16': { t: 'BF16 TFLOPS', d: 'Dense tensor-core throughput in bfloat16, no sparsity. Vendors often quote twice this "with sparsity".', typ: '300 (A100), 990 (H100), 2,250 (B200).', lo: '', hi: '' },
  'tflops-fp8':  { t: 'FP8 TFLOPS', d: 'Dense 8-bit floating-point tensor throughput; usually 2× BF16 on chips that have it.', typ: '', lo: '', hi: '' },
  'tflops-fp4':  { t: 'FP4 TFLOPS', d: 'Dense 4-bit floating-point tensor throughput (NVFP4/MXFP4); Blackwell and CDNA 4 only.', typ: '', lo: '', hi: '' },
  bw:            { t: 'Memory bandwidth', d: 'HBM (or GDDR/LPDDR) bandwidth in GB/s. Decode speed per user scales almost directly with it.', typ: '2 TB/s (A100), 3.35 (H100), 4.8 (H200), 8 (B200/MI350).', lo: '', hi: '' },
  generation:    { t: 'Chip generation', d: 'The architecture family (NVIDIA compute capability, AMD gfx target, TPU version). It decides which number formats are computed natively.', typ: '', lo: '', hi: '' },
  'native-fmt':  { t: 'Native formats', d: 'Number formats the tensor cores compute directly; running weights in one of them speeds up both prefill and decode.', typ: 'BF16 everywhere; FP8 from Ada/Hopper/MI300; FP4 on Blackwell and CDNA 4.', lo: '', hi: '' },
  'weight-only': { t: 'Weight-only formats', d: 'Formats the engine can store weights in but must dequantize to BF16 before each matmul: memory shrinks, compute does not speed up.', typ: 'INT4 AWQ/GPTQ on every NVIDIA generation; FP8/FP4 via Marlin on Ampere and Hopper.', lo: '', hi: '' },
  price:         { t: 'Price per hour', d: 'Approximate on-demand cloud rate per accelerator, read from the linked price page; edit it to your contract.', typ: '$1.5 (A100), $2.5–3 (H100), $5–6 (B200).', lo: '', hi: '' },
  params:        { t: 'Parameters', d: 'Total weights in billions; sets memory for the weights.', typ: '', lo: '', hi: '' },
  active:        { t: 'Active parameters', d: 'Parameters used per token in a mixture-of-experts model; sets compute per token and, at small batch, the bytes read per step.', typ: '3–40B active for 30B–1T total.', lo: '', hi: '' },
  layers:        { t: 'Layers', d: 'Transformer blocks; each holds attention (with KV cache) and an MLP or expert block. Collectives per token scale with it.', typ: '32–128.', lo: '', hi: '' },
  heads:         { t: 'Heads / KV heads', d: 'Query heads and shared key-value heads (grouped-query attention). Fewer KV heads mean a smaller cache per token; MLA replaces them with one compressed latent.', typ: '8 KV heads on most dense models; 4 on Qwen3 MoE; 2 on Qwen3-Next.', lo: '', hi: '' },
  'kv-1m':       { t: 'KV per 1M-token session', d: 'What one million tokens of context cost in KV cache, windowed layers included. The most honest single number for long-context suitability.', typ: '25 GB (Qwen3-Next) to 516 GB (Llama 405B) in BF16.', lo: '', hi: '' },
  'max-ctx':     { t: 'Max context', d: 'Largest context the released weights support, sometimes only with RoPE scaling beyond the native window (shown in brackets).', typ: '128k common; 256k–1M for recent long-context models.', lo: '', hi: '' },
  'attn-layers': { t: 'Attention layers', d: 'How many layers use full attention, sliding-window or chunked attention (KV capped at the window), multi-head latent attention, or linear attention (no KV cache).', typ: '', lo: '', hi: '' },
  'ships-in':    { t: 'Ships in', d: 'Precision of the official checkpoint. Serving above it upcasts for nothing; serving below it needs a quantized checkpoint.', typ: 'BF16 for most; FP8 (DeepSeek, Kimi); MXFP4 (gpt-oss).', lo: '', hi: '' },
  verified:      { t: 'Verified against', d: 'The Hugging Face repository whose config.json the shape was checked against (tools/import-model.js --check).', typ: '', lo: '', hi: '' },
  checkpoints:   { t: 'Known checkpoints', d: 'Quantized variants that exist on Hugging Face with the same shape and the claimed format, found and re-verified by the import tool.', typ: '', lo: '', hi: '' },
  'per-node':    { t: 'Per node', d: 'Accelerators sharing one fabric in this vendor\'s standard node.', typ: '', lo: '', hi: '' },
  tdp:           { t: 'TDP', d: 'Rated power per accelerator in watts.', typ: '300–1,400 W.', lo: '', hi: '' },
  'mem-gb':      { t: 'Memory', d: 'Accelerator memory in decimal gigabytes (1e9 bytes), the way vendors quote it.', typ: '', lo: '', hi: '' },
  nodes:         { t: 'Nodes', d: 'Servers needed for the layout at the accelerators-per-node of this hardware.', typ: '', lo: '', hi: '' },
  'layout-col':  { t: 'Layout', d: 'Tensor parallel × pipeline parallel × replicas, plus any separate prefill pool.', typ: '', lo: '', hi: '' },
  // ledger options
  'opt-kv-fp8':  { t: 'KV cache in FP8', d: 'Store keys and values as 8-bit floats: half the cache per token, so twice the sessions or context, with rarely measurable quality loss.', typ: 'The first optimization to switch on.', lo: '', hi: '' },
  'opt-kv-int4': { t: 'KV cache in INT4', d: 'Four-bit keys and values: a quarter of the cache per token. Quality risk grows with context and few engines offer it.', typ: '', lo: '', hi: '' },
  'opt-w-fp8':   { t: 'Weights in FP8', d: 'Halves the weight bytes and, on hardware with FP8 tensor cores, speeds up prefill and decode; quality is close to BF16 for most models.', typ: 'Default on Hopper and newer.', lo: '', hi: '' },
  'opt-w-4bit':  { t: 'Weights in 4 bits', d: 'INT4 (AWQ/GPTQ) everywhere as weight-only, or NVFP4 natively on Blackwell: a quarter of the weight bytes, at some quality cost for small models.', typ: '', lo: '', hi: '' },
  'opt-host':    { t: 'Park idle sessions in host memory', d: 'Keep only in-flight sessions on the accelerators and copy idle ones to CPU memory, restoring them over PCIe on the next turn.', typ: '', lo: '', hi: '' },
};
if (typeof module !== 'undefined') module.exports = { GLOSSARY };
